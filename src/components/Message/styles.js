import styled from 'styled-components';

export const Container = styled.span`
    position: absolute;

    z-index: 2;
    bottom: 0;
    color: red;
    font-size: 14px;
    text-transform: capitalize;
`;
